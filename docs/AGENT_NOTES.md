# Quicky — Agent Notes (ground truth for subagents)

> Last checked: 2026-08-13. Branch `feat/dashboard-tile-polish` (up to date with origin).
> Stack/architecture authority: `STACK.md` (read it before touching routing, theme, or launchers).

## Test status: GREEN ✅
- `flutter test` → **9 passed, 0 failed** (exit 0).
- Suites:
  - `test/widget_test.dart` (1): app boots + splash.
  - `test/features/dashboard_test.dart` (6): DashboardPage 8 tiles + settings hub, OctagonTile accent/tappable, DashboardLayout no-overflow at small viewport, OctagonTile 320px + keyboard WCAG 2.1 AA.
  - `test/features/controllers_test.dart` (2): AttractionsController radii constant, SettingsState copyWith isolates fields.
- `flutter test --coverage` ran clean; `coverage/lcov.info` regenerated (new untracked artifact).
- Gate order: `flutter analyze` → `flutter test`. Lint rules relaxed in `analysis_options.yaml` (public_member_api_docs + sort_pub_dependencies disabled — private app).

## Untracked (do not commit without asking)
- `docs/Bluetooth connection.txt`
- `run_quicky.sh`
- `coverage/lcov.info` (generated by coverage run)

## Do-not-break contracts (from STACK.md)
- `lib/core/router/app_router.dart` is CENTRAL — append routes in `routes.dart`, never edit per-feature.
- Dashboard = 8 octagon tiles at 45° intervals + center Settings button. Keep tile count at 8.
- App launchers: Android package id is the authoritative installed-check target; URL schemes are unverified community values (verify on-device). Banking/BOLT launchers must be installed-app-only (show "Not installed", never link to store).
- i18n fail-fast: missing ARB key should break the build, not render blank.

## Open threads for next agent
- Bluetooth: `docs/Bluetooth connection.txt` is untracked and unread here — likely a feature spec/notes. Read it before any Bluetooth work.
- `run_quicky.sh` is a convenience runner; confirm it matches current `flutter run` flags before relying on it.
